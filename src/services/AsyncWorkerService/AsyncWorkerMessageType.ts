/**
 * Enumerates the asynchronous worker message types.
 */
enum AsyncWorkerMessageType {
    /**
     * Respond to an asynchronous request.
     */
    Respond = 'AsyncWorkerService/Respond',
}

export default AsyncWorkerMessageType;
