/**
 * Enumerates the unique identifiers of all commands.
 */
enum CommandId {
    Boot = 'boot',
    Child = 'child',
    Elevate = 'elevate',
    Fork = 'fork',
    Login = 'login',
    Overclock = 'overclock',
    Scan = 'scan',
    ScanFiles = 'scan-files',
    ScanHub = 'scan-hub',
}

export default CommandId;
