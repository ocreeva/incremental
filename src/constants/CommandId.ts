/**
 * Enumerates the unique identifiers of all commands.
 */
enum CommandId {
    Boot = 'boot',
    Child = 'child',
    Fork = 'fork',
    Login = 'login',
    Overclock = 'overclock',
    Scan = 'scan',
    ScanHub = 'scan-hub',
}

export default CommandId;
